---
description: 
globs: 
alwaysApply: true
---
---
description: 该规则解释了在编写各类typescript内容时的一些通用的编码习惯
globs: **/*.jsx,**/*.tsx,**/*.vue,**/*.ts,**/*.js
alwaysApply: false
---

# JavaScript/TypeScript编码习惯

## 代码风格

- 不添加js行末的分号
- 通常使用双引号
- import语句的解构内容不允许换行，写在同一行！！！如果内容过长，允许换行，但每一行需要写多个解构项，保持纵向长度较小
- Object或Array的解构不允许换行，写在同一行！！！如果内容过长，允许换行，但每一行需要写多个解构项，保持纵向长度较小
- Object或Array的构造，如果中小规模，可以选择不换行或每个属性一行的写法；如果是大规模，禁止每个属性一行，需要每一行写多个构造项，保持纵向长度较小
- 函数的`function<>({}: {})`行禁止换行。泛型参数再多也写一行；参数解构如果过多，使用在函数内解构参数的写法；参数类型如果过长，单独提取类型
- 简单接口类型优先使用`interface {}`而不是`type = {}`
- 类型定义interface或type中的`{ ... }`定义允许且优先使用一个属性一行的写法，除非该接口定义非常简单，此时不换行将所有属性写在一行