---
description: 
globs: 
alwaysApply: true
---
---
description: 该规则解释了在编写各类前端, 诸如vuejs, react时的一些通用的编码习惯
globs: **/*.jsx,**/*.tsx,**/*.vue
alwaysApply: false
---

# 前端规范

## 代码风格

- 不添加js行末的分号
- 禁止「一个属性占一行」的写法；属性较少时应整行书写，属性较多时每行应包含多个属性，必要时用变量/小函数收起长回调以缩短 JSX
- 组件函数的`function<>({}: {})`行禁止换行。泛型参数再多也写一行；props解构如果过多，使用在函数内解构props的写法；Props类型如果过长，单独写一个Props类型
- 禁止在 JSX 行内编写回调函数，除非该函数十分简单，且该组件场景下无法避免每次render时该回调函数的重建，否则都要提到前面且必要时使用useCallback
- 组件拆解时，遵循自大而小的书写规则，较大的集成组件卸载前面，它的细分组件和工具函数写在后面